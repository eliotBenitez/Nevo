- Улучшены отступы в панели «О программе»: добавлен верхний отступ для кнопок действий (`about-actions`).
- Добавлено автообновление приложения на базе Tauri updater (plugin-updater + plugin-process): тихая проверка при запуске и ручная проверка кнопкой в разделе «О программе».
- Добавлен компонент `UpdateDialog.vue` в стиле приложения (доступно обновление, заметки к релизу, прогресс загрузки, перезапуск, статусы «актуально»/ошибка) и композабл `useAppUpdater.ts` с общим состоянием.
- Добавлен GitHub Actions workflow `release.yml` для сборки под все платформы, подписи артефактов и публикации релиза с манифестом `latest.json`.
- Настроено: `createUpdaterArtifacts`, endpoint GitHub Releases и публичный ключ подписи в `tauri.conf.json`; разрешения `updater`/`process` в capabilities; регистрация плагинов в `lib.rs`.
- Добавлены строки локализации `updater.*` (ru/en) и обновлён текст «О программе».
- Исправлено рассогласование версий: `Cargo.toml`, `package.json` и `tauri.conf.json` приведены к `0.1.1`.
