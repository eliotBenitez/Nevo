- Добавлено закрытие модальных окон Истории (`WorkspaceHistoryModal.vue`) и Корзины (`WorkspaceShell.vue` / `WorkspaceTrashBin`) при помощи клавиши Escape с предотвращением дальнейшего всплывания события.
- Добавлены соответствующие юнит-тесты во `WorkspaceHistoryModal.test.ts` и `WorkspaceShell.test.ts` для автоматической проверки закрытия модальных окон по клавише Escape.
- Добавлены горячие клавиши по умолчанию для открытия системных представлений:
  - Граф (`workspace.open-graph`): `Ctrl+Alt+G`
  - История (`workspace.open-history`): `Ctrl+Alt+H`
  - Корзина (`workspace.open-trash`): `Ctrl+Alt+T`
- Интегрированы горячие клавиши в настройки и в поиск настроек на бэкенде и фронтенде, добавлены соответствующие переводы в файлы локализации `en.json` и `ru.json`.
- Добавлены горячие клавиши по умолчанию `Ctrl+Alt+\\` для переключения правой панели (`workspace.toggle-right-panel`) на бэкенде Rust (`types.rs`) и на фронтенде (`defaults.ts`, `hotkeys.ts`, `useWorkspaceKeymap.ts`).
- Интегрировано отображение и кастомизация новой горячей клавиши в настройках горячих клавиш (`useSettingsHotkeys.ts`, `settings-helpers.ts`).
- Улучшен внешний вид правой панели (`WorkspaceRightPanel.vue`): добавлен боковой отступ (padding) 12px для элемента `.right-panel__empty` (состояние, когда оглавление пустое), чтобы выровнять его по уровню с заголовком панели. Внутри секций с собственными отступами боковой отступ для пустых состояний сбрасывается для консистентного отображения.
- Исправлен триггер правой панели (`WorkspaceRightTrigger.vue`): событие переключено с `click` на `mousedown.stop.prevent` для повышения надежности, увеличена ширина активной области по умолчанию с 8px до 12px (до 24px при наведении) и снижено время анимации до 120мс, что решило проблему неоткрытия панели при быстрых кликах.
- Добавлена кнопка переключения правой панели в шапку (`WorkspaceShell.vue`) рядом с поиском для симметричного и интуитивного управления видимостью панели, аналогичного левой боковой панели.
- Добавлены переводы для переключателя правой панели в файлы локализации `en.json` и `ru.json`.
- Исправлена ошибка при сохранении настроек рабочей области (`save_workspace_settings`): добавлена десериализация по умолчанию для поля `showBacklinksByDefault` на бэкенде Rust и возвращено поле в TypeScript интерфейсы, дефолты и normalizers на фронтенде для обеспечения совместимости.
- Исправлено: правая панель (`WorkspaceRightPanel.vue`) больше не открывается автоматически при открытии или переключении заметок.
- Удалена неиспользуемая настройка `showBacklinksByDefault` из настроек навигации, параметров пространства, локализации и поискового индекса параметров.
- Добавлен метод `registerBlockType` в API плагинов: кастомный блок (узел + node view + поповер + сериализация + импорт + slash-пункт) теперь регистрируется одним вызовом; добавлены гранулярные методы `registerNodePopover`, `registerNodeSerializer`, `registerNodeImporter`, `requestNodeEdit`.
- Добавлены декларативные поповеры редактирования для плагинных блоков: разработчик описывает поля (`text`, `textarea`, `select`, `number`, `checkbox`, `color`), а форму, позиционирование и сохранение в `attrs` строит сам редактор (`EditorPluginNodePopover.vue`, `usePluginNodePopover.ts`).
- Исправлена потеря плагинных узлов при импорте/экспорте: экспортёры Markdown/HTML/Typst и импортёр Markdown теперь учитывают плагинные сериализаторы/импортёры через активный реестр (`active-serialization.ts`), обеспечивая round-trip.
- Добавлены юнит-тесты `plugin-block-type.test.ts` (регистрация блока, сериализация и импорт), обновлено руководство `docs/plugins.md`.
- Исправлено отображение блока «Что нового» в окне обновления: вместо названия релиза теперь показывается его полное описание, загружаемое из GitHub Releases API (с фолбэком на заметки из манифеста обновления).
- Добавлен лёгкий рендер Markdown без новых зависимостей (`releaseNotesMarkdown.ts`) с экранированием HTML и поддержкой списков, заголовков, жирного/курсива, инлайн-кода и http(s)-ссылок; покрыт юнит-тестами.
- Улучшен внешний вид окна обновления: иконка-бейдж и бейдж версии в шапке, оформленная прокручиваемая область заметок, анимация появления, ловушка фокуса и закрытие по Esc.
- Исправлено поведение клавиши Enter в slash-меню редактора, из-за которого выбирался не тот элемент, который был визуально подсвечен. Теперь порядок элементов в ProseMirror-состоянии совпадает с порядком визуального отображения (группировкой по категориям).
- Добавлен юнит-тест в `slash.test.ts` для проверки корректности группировки элементов по категориям.
