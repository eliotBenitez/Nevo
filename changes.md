## [2026-06-09]
- Исправлено применение пользовательских стилей (`custom.css`): `<style>` теперь инъектируется в конец `<body>` и удерживается последним через `MutationObserver`, поэтому правила выигрывают каскад по порядку и не перебиваются стилями приложения/SFC.
- Улучшено: флажок «Пользовательские стили» в настройках пространства переведён с сырого `<input type="checkbox">` на примитив `NvToggle`.

## [2026-06-09]
- Улучшено применение пользовательских стилей (`custom.css`): теперь при включении тумблера в настройках стили считываются с диска динамически, что позволяет применять изменения без перезапуска приложения.
- Удален редактор пользовательских CSS стилей (`textarea`) из модального окна настроек пространства; стили теперь считываются и применяются непосредственно из хранилища пространства (`custom.css`).
- Из файлов локализации `en.json` и `ru.json` удалены неиспользуемые ключи: `editorTitle`, `editorDescription`, `placeholder` для пользовательских стилей.
- Исправлены strict TypeScript ошибки компиляции в `WorkspaceEditorPane.test.ts` (добавлены поля `customCssEnabled` и `customCssFileName`) и в `settings-workspace-items.ts` (удалена неиспользуемая переменная `coming`).

## [2026-06-09]
- Исправлена ошибка компиляции `vue-i18n` в настройках воркспейса из-за неэкранированных фигурных скобок в плейсхолдере custom CSS.
- Добавлен тест в `src/i18n.test.ts` для проверки компиляции плейсхолдера с кастомным CSS.

## [2026-06-09]
- Добавлена поддержка пользовательских CSS стилей (`custom.css`) для каждого пространства с возможностью редактирования непосредственно в настройках.
- Добавлены новые Tauri/Rust команды `load_custom_css` и `save_custom_css` для чтения и записи `.nevo/custom.css` в директории пространства.
- Добавлена поддержка облачной синхронизации стилей Custom CSS через ydoc метаданные Yjs для Cloud-воркспейсов.
- Перенесен блок настроек оформления пространства (акцентный цвет, сцена, поверхности, контрастность, сайдбар) из общей панели «Внешний вид» в настройки конкретного пространства.
- Добавлено сохранение типа (`workspaceType`) и статуса (`status`) пространства в интерфейсе настроек.
- Добавлена поддержка автоматического разворачивания/сворачивания сайдбара на основе настройки `sidebarDefaultState`.
- Добавлено автоматическое открытие правой панели со списком обратных ссылок при открытии заметки, если включена опция `showBacklinksByDefault`.
- Добавлена инициализация отображения подписей графа на основе настройки `showGraphLabels`.
- Добавлено скрытие пустых папок в боковой панели при выключенной опции `showEmptyFolders`.
- Добавлена автоматическая фокусировка графа на текущей заметке при включенной опции `graphEntryMode: 'from-current-note'`.
- Добавлено запоминание свернутых папок в боковой панели (`rememberExpandedFolders`) с сохранением состояния в localStorage для каждого пространства.
- Удалены избыточные и нереализованные опции из интерфейса и поиска настроек: `openLastVisitedSystemView`, `defaultChildSort`, `graphScopeDefault`, `searchStartScope`, `historyDefaultRange`.
- Исправлен вызов Pinia store в компоненте `GraphView.vue`, сделав его ленивым (внутри `onMounted` and `watch`), чтобы предотвратить падение тестов `graph-focus-mode.test.ts`.
